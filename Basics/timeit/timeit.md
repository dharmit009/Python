# Timeit Module: 

**Why timeit and not time?**

`timeit` runs the snippet of code multiple times roughly a ten thousand
times and provides an average of the same. The `time` module does not
offer such functionality therefore, it is recommended to use `timeit`
module instead of `time`.



